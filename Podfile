# Uncomment the next line to define a global platform for your project
platform :ios, '11.4'
use_frameworks!

# Pods for Quite-prac
def install_pods
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'
end

target 'Qiita-prac' do
 install_pods
end
